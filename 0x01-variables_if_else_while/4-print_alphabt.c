#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	char alphabet;

	for (aplhabet = 'a'; alphabet <= 'z', alphabet++)
	{
		if (alphabet != 'e' && letter != 'q)
			putchar(letter);
	}

	putchar('\n');

	return (0);
}
